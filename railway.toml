[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:8080 --timeout 600 --preload"
healthcheckPath = "/health"
healthcheckTimeout = 600
