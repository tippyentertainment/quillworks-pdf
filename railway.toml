[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:8080 --timeout 120 --preload"
healthcheckPath = "/health"
healthcheckTimeout = 120
